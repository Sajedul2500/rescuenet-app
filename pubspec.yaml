name: RescueNetApp
description: A new Flutter project.

publish_to: "none" # Remove this if you plan to publish your package

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  font_awesome_flutter: ^10.7.0
  url_launcher: ^6.2.6
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  geolocator: ^11.0.0
  permission_handler: ^11.2.0
  http: ^1.2.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/rescuenet_logo.png
